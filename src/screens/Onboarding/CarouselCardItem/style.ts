import { StyleSheet } from 'react-native';
import { ITEM_WIDTH } from '../style';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
  },
  image: {
    width: ITEM_WIDTH,
    height: 300,
  },
  cardFooter: {
    marginTop: 50,
  },
});
